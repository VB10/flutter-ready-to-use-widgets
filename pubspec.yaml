name: ready_to_use_widgets
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.16.1 <3.0.2'

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  syncfusion_flutter_charts: ^20.1.50
  vexana: ^2.8.0
  #state management
  bloc: ^8.0.3
  flutter_bloc: ^8.0.1 
  
  #utility
  internet_connection_checker: ^0.0.1+3

dev_dependencies:

  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
